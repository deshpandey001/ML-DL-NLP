version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:5000"
    depends_on:
      - redis
    command: flask run --host=0.0.0.0 --port=5000
    environment:
      - FLASK_ENV=development

  redis:
    image: redis:alpine
